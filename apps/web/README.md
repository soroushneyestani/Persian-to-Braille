# Persian Braille Web

Official browser-based playground/application.

Responsibilities:
- provide interactive translation and specification-inspection workflows;
- consume the public SDK;
- keep UI concerns outside the core engine.

Dependency rule:
- may depend on `packages/sdk`;
- must not redefine specification behavior.
